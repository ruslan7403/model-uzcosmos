# Model weights

- **`traffic_sign_yolo.pt`** – YOLO detector for traffic signs (single class). Use with `run_detection_draw_rects.py` or `run_archive_20_detection.py` via `--yolo-model models/traffic_sign_yolo.pt`.
